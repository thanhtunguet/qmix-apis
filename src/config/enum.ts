// Enum for User Roles
export enum UserRole {
  ADMIN = 'Admin',
  MODERATOR = 'Moderator',
  TEACHER = 'Teacher',
  STUDENT = 'Student',
}

// Enum for Question Types
export enum QuestionType {
  MCQ = 'MCQ',
  FILL_IN_THE_BLANKS = 'FillInBlanks',
  WORD_FORM_TRANSFORMATION = 'WordFormTransformation',
  PREPOSITION = 'Preposition',
  ERROR_CORRECTION = 'ErrorCorrection',
  READING_COMPREHENSION = 'ReadingComprehension',
  SENTENCE_TRANSFORMATION = 'SentenceTransformation',
  SENTENCE_REWRITING = 'SentenceRewriting',
}

// Enum for Gender
export enum Gender {
  MALE = 'M',
  FEMALE = 'F',
}

// Enum for Answer Codes
export enum AnswerCode {
  A = 'A',
  B = 'B',
  C = 'C',
  D = 'D',
}
